from flask import request

class CadastroBusiness():

    def valida_body_request(self, request):
        data = request.get_json()

        if

        # primeiro verifica o nome dos objetos
        # depois valida os parametros
        # caso nao esteja, retornar mensagem com nome
        # do objeto

        for obj in data:
            if not data['pessoa']:


            if not data['usuario']:
                pass


        return data